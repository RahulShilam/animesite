const AnimeInfoLayout = ({ children }) => {
  return (
    <div style={{ color: "white" }}>
      {/* What are we doing here */}
      <div>{children}</div>
    </div>
  );
};
export default AnimeInfoLayout;
